{
  "name": "data-platform-lab",
  "version": "0.1.0",
  "description": "Hands-on data engineering lab — batch pipelines, streaming, validation, storage, and orchestration exercises.",
  "type": "module",
  "license": "MIT",
  "packageManager": "yarn@4.6.0",
  "engines": {"node": ">=22.0.0"},
  "bin": {"data-platform-lab": "src/cli/main.js"},
  "scripts": {
    "test": "node --test 'tests/*.test.js'",
    "lint": "eslint src/ tests/",
    "lint:fix": "eslint --fix src/ tests/",
    "check": "node --check src/**/*.js"
  },
  "dependencies": {"@aws-sdk/client-s3": "^3.0.0"},
  "devDependencies": {"@eslint/js": "^9.0.0", "eslint": "^9.0.0"}
}
